class Solution {
    public int numOfStrings(String[] patterns, String word) {
        int count=0;
        for(int i=0; i<patterns.length; i++)
        {
                if(word.contains(patterns[i]))
                {
                    count++;
                }//if the word contains substring the counter will increase 
        }
        return(count);
    }
}
